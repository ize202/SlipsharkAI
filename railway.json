{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && mkdir -p app/Functions app/functions && cp -n app/Functions/llm_functions.py app/functions/ 2>/dev/null || cp -n app/functions/llm_functions.py app/Functions/ 2>/dev/null || true"
  },
  "deploy": {
    "startCommand": "python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
} 